package com.seo.seotoolshub.repository;

import com.seo.seotoolshub.model.GeneratorResult;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GeneratorResultRepository extends JpaRepository<GeneratorResult, Integer> {
}